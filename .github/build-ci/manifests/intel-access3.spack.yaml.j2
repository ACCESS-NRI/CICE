spack:
  specs:
  - access-cice @git.{{ ref }} +access3 io_type=PIO
  packages:
    all:
      require:
      - '%{{ intel_compiler }} target={{ target }}'
  concretizer:
    unify: false
  view: false
