name: Build
on:
  pull_request:
  push:
    branches:
      - main
      - CICE6.*
jobs:
  pre-ci:
    name: Pre-CI
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - uses: actions/checkout@v7

      - name: Set up matrix
        id: set-matrix
        # Find all relevant files under .github/build-ci/manifests
        # then output them as a JSON array (minus the last comma)
        run: |
          files=$(find .github/build-ci/manifests/ -iname '*.j2' -printf '"%p",')
          echo "matrix=[${files%,}]" >> $GITHUB_OUTPUT

  ci:
    name: CI
    needs: pre-ci
    strategy:
      fail-fast: false
      max-parallel: 5
      matrix:
        file: ${{ fromJson(needs.pre-ci.outputs.matrix) }}
        # TODO: Builds were failing due to https://github.com/ACCESS-NRI/access3-share/pull/9, we need this added to the access3-share spack package - (https://github.com/ACCESS-NRI/spack-packages/pull/290)
        exclude:
          - file: .github/build-ci/manifests/intel-access3.spack.yaml.j2
          - file: .github/build-ci/manifests/oneapi-access3.spack.yaml.j2
    uses: access-nri/build-ci/.github/workflows/ci.yml@v4
    with:
      spack-manifest-path: ${{ matrix.file }}
      allow-ssh-into-spack-install: false  # If true, PR author must ssh into instance to complete job
      # More args, including setting spack/spack-config refs, can be found in https://github.com/ACCESS-NRI/build-ci/blob/v4/.github/workflows/README.md
